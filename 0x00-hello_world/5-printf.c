#include <stdio.h>
/**
 * main-entry point for the program.
 *
 * Return:o if no errors, return non zero if errors
 */

int main(void)
{
printf("with proper grammer, but the outcome is a piece of art,/n");
return (0);
}
